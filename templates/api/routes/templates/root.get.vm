#if($input.params('from') == 'all')
{   
    "Command":"EXPORT",
    "Filter":"$input.params('filter')",
    "Query":"$input.params('query')"
}
#elseif($input.params('query').length()>0)
{
    "Command":"SEARCH",
    "Query":"$input.params('query')",
    "From":"$input.params('from')",
    "Topic":"$input.params('topic')"
}
#else
{
    "Command":"LIST",
    "From":"$input.params('from')",
    "Filter":"$input.params('filter')",
    "Perpage":"$input.params('perpage')"
}
#end
