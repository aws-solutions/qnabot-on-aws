# Import Lambda
this lambda imports QnAs from S3 into elasticsearch,

## Tests
to setup environment for tests run:
```shell
./test/setup.sh
```

test are run using:
```shell
npm test
```
or
```shell
npm unit {{test-name}}
```

