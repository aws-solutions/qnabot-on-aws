# Lex-Build Lambda
Rebuilds AWS Lex Bot

## Tests
to setup environment for tests run:
```shell
./test/setup.sh
```

test are running using:
```shell
npm test
```
or
```shell
npm unit {{test-name}}
```

