#set ($root="https://${!context.domainName}/${!context.stage}")

{
    "bucket":"${ImportBucket}",
    "prefix":"status/",
    "perpage":"$input.params('perpage')",
    "token":"$input.params('token')",
    "type":"imports",
    "root":"$root"
}

